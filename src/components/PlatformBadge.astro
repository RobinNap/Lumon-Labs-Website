---
interface Props {
  platform: 'iOS' | 'iPadOS' | 'macOS' | 'watchOS' | 'visionOS' | 'tvOS';
}

const { platform } = Astro.props;

const icons: Record<string, { emoji: string; label: string }> = {
  iOS: { emoji: '📱', label: 'iPhone' },
  iPadOS: { emoji: '📱', label: 'iPad' },
  macOS: { emoji: '💻', label: 'Mac' },
  watchOS: { emoji: '⌚', label: 'Apple Watch' },
  visionOS: { emoji: '🥽', label: 'Apple Vision Pro' },
  tvOS: { emoji: '📺', label: 'Apple TV' },
};

const info = icons[platform] || { emoji: '📱', label: platform };
---

<span 
  class="inline-flex items-center gap-1.5 px-3 py-1.5 bg-white/5 border border-white/10 rounded-full font-sans text-sm"
  title={info.label}
>
  <span>{info.emoji}</span>
  <span class="text-white/60">{info.label}</span>
</span>
